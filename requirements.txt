# RSS 解析
feedparser==6.0.11

# HTTP 请求
requests==2.31.0
# 伪装浏览器 TLS 指纹，用于绕过部分 RSS 服务的 WAF/Cloudflare 拦截
curl_cffi>=0.7.0

# 命令行工具
click==8.1.7
rich==13.7.1

# 环境变量
python-dotenv==1.0.1

# AI SDK
openai==1.30.1
# httpx 新版本（>=0.28）不兼容 openai 1.30.1，会报 'proxies' 参数错误
httpx==0.27.2

# Token 计数
tiktoken==0.7.0

# 文本处理
beautifulsoup4==4.12.3
lxml==5.2.1

# 配置文件解析
pyyaml==6.0.1
